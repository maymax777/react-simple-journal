import styled from 'styled-components';

const CardDate = styled.span`
  font-family: 'Inter';
  font-weight: 500;
  font-style: normal;
  font-size: 16px;
  line-height: 19.36px;
  letter-spacing: -2%;
  color: #333;
`;

export default CardDate;
