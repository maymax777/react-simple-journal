import styled from 'styled-components';

const Title = styled.div`
  position: absolute;
  top: 24px;
  right: 46px;

  font-family: 'Inner';
  font-style: normal;
  font-weight: 400;
  font-size: 24px;
  line-height: 29px;
  letter-spacing: -0.02em;

  color: #f6f4f6;
`;

export default Title;
